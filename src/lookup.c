#include "lookup.h"
#include <stdio.h>

int anzahlEintraege = 0;

int ladeLookupTabelle(const char* dateiname) {
    // Dummy, damit Programm läuft ohne CSV
    return -1; 
}

float berechneAbstand(int messwert) {
    return (float)messwert; 
}